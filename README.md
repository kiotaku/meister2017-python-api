# meister2017-python-api

## How to run

    python -m pip install django
    cd ./counter/
    python manage.py loaddata ./access_count/fixture/initial_data.yaml
    python manage.py runserver # running to port 8000
